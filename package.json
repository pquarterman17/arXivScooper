{
  "name": "arxiv-poop-scooper",
  "version": "0.1.0",
  "private": true,
  "description": "Frontend modules for arXivScooper — a scientific literature manager for SCQ (superconducting quantum computing) research. No build step — vanilla ES modules served by scq/server.py.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "vite build --config vite.build.config.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "jsdom": "^24.0.0",
    "sql.js": "^1.10.0",
    "typescript": "^5.4.0",
    "vite": "^8.0.10",
    "vitest": "^4.1.5"
  }
}
